import React from 'react';

export default function KasirLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen bg-[#f9fafb] font-sans">
      {/* Kasir layout placeholder */}
      {children}
    </div>
  );
}
